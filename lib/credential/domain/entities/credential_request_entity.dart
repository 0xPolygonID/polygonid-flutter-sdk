class CredentialRequestEntity {
  final String identifier;
  final String url;
  final String id;
  final String thid;
  final String from;

  CredentialRequestEntity(
      this.identifier, this.url, this.id, this.thid, this.from);
}
